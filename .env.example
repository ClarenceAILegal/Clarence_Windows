# Shared password required to use the private website
MOTION_BOT_PASSWORD=change-me-to-a-strong-password

# Optional but recommended: stable signing key for session cookies
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
MOTION_BOT_SECRET_KEY=
