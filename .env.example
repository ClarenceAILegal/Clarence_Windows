# Optional override of the default site password (B0ts4Justice)
# MOTION_BOT_PASSWORD=B0ts4Justice

# Optional but recommended: stable signing key for session cookies
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
MOTION_BOT_SECRET_KEY=
